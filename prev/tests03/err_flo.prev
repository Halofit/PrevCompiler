
# free flow functions are erronous - kinda bad imo
# -> better would be to allow global defines along with while (like lisp)

# also this is a copy of the last weeks test, since ast includes no new failure cases
fun three() : integer = 3